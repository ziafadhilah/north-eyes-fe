export interface AreaData {
  area_name: string;
  logo_url: string;
  outlet_id: string;
  description: string;
  area_id: string;
}

export interface CreateAreaData {
  area_name: string;
  logo_url: string;
  outlet_id: string;
  description: string;
}

export interface EditAreaData {
  area_name: string;
  logo_url: string;
  outlet_id: string;
  description: string;
  area_id: string;
}
