export interface SlugData {
  slug_name: string;
  slug: string;
  slug_id: number;
}

export interface CreateSlugData {
  slug_name: string;
  slug: string;
}

export interface EditSlugData {
  slug_name: string;
  slug: string;
  slug_id: number;
}
